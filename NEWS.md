# covr2gh (development version)

* `use_covr2gh_action()` adds the `covr2gh.yaml` workflow file and inserts a
README badge with `use_covr2gh_badge()`.
* `compose_comment()` and `post_comment()` produce and post the comment.
* `generate_badge` makes the badge.
